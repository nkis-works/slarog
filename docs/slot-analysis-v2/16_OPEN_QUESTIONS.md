# Open Questions

設計レビューで製品仕様の主要論点は固定済み。以下は公開運用または後続案件で判断する事項で、v2 domain実装のブロッカーではない。

## 解決済み

- Release Core: クイックだけを先行公開せず、[04_FEATURE_MASTER.md](./04_FEATURE_MASTER.md) のRC-01〜RC-16を全完成してから新UIへ切り替える。
- 基準丸め: 厳密値でrelationを決め、表示0枚かつ厳密非0は`差は1枚未満（上回る/下回る）`。
- 回復名称: UIは`下落後の最大回復`。下落前の初期run-upは含めない。
- 累積開始点: 0G/0枚に限定せず、任意の開始地点を許す。
- 区間上限: UI 10区間、累積初期候補11地点、domain 100区間。
- 現行コイン持ち: 削除せずRelease Coreの段階導線に残す。
- 名称/slug: H1 `スロット出玉分析`、nav `出玉分析`、slug `/tools/slot-analysis/`。
- SEO: title/descriptionを固定し、`機械割`はSEO、FAQ、補足だけに使う。

## Q1 — 正式名称の法的確認

- 現状: `スロット出玉分析`を製品仕様として固定。J-PlatPatの文字列部分一致は0件。
- 必要判断: 公開URL/H1変更前に、指定商品/役務と類似称呼を含む専門家確認を行うか。

## Q2 — 新URLへの移行タイミング

- 固定方針: previewでRelease Coreを完全QA後、名称、canonical、sitemap、redirectを同一リリースで変更する。
- 残作業: Cloudflare redirects構文と旧URLの実アクセス確認。domain PRでは扱わない。

## Q3 — Slarog CTAのリンク先と文言

- 必要: 本番で既存ストア/製品導線のどれを使うか、実装済み機能だけを説明しているか確認する。
- 制約: 自動転送、履歴同期、共有を示唆しない。

## Q4 — 広告実験の開始条件

- 現状: OFF。広告DOM、通信、予約空白を置かない。
- 必要: AdSense審査、地域、CMP、privacy、CSP、ads.txt、publisher IDを別案件で承認する。

## Q5 — 利用者テストの実施方法

- 推奨: 経験差のある5名以上とアクセシビリティ観点1名。実データは収集しない。
- 代替: 公開前の内部同等評価を記録し、P0/P1を0にする。

## 承認時チェック

- [x] 名称、nav、slug
- [x] Release Core境界
- [x] 丸め境界
- [x] 最大回復の語と定義
- [x] 累積開始点と区間上限
- [ ] 正式名称の公開前法的確認
- [ ] 旧URL転送の実装確認
- [ ] Slarog CTA事実確認
- [x] 広告は別案件
